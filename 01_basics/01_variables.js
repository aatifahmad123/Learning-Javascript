const accountId = 123

let accountEmail = "ahmadaatif6@gmail.com"

var accountPassword = "Typescript-Go"

// Prefer not to use var due to block scoping and functional scoping

/*

In JavaScript, let and var both declare variables, but let is block-scoped (accessible within the block where it's declared), while var is function-scoped (accessible throughout the function where it's declared).
 
*/

accountCity = "Jaipur"

accountEmail = "b22ai002@iitj.ac.in"
accountPassword = "React-Redux"
accountCity = "Bengaluru"

let accountState;

console.table([accountEmail, accountId, accountPassword, accountCity, accountState])